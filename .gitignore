# Vendored source repositories — read-only reference copies, not part of this project
/target/
.env
/.codex/

# Temporary feature workspace — working artifacts are not committed
# (see AGENTS.md "Temporary Feature Workspace")
/.work/
